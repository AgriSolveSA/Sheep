{
  "name": "shepherdai-server",
  "version": "1.0.0",
  "description": "ShepherdAI backend — Express + SQLite",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "init-db": "node db/init.js",
    "seed": "node scripts/seed.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "better-sqlite3": "^9.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "nodemailer": "^6.9.13",
    "pdfkit": "^0.15.0"
  },
  "devDependencies": {
    "pm2": "^5.3.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
